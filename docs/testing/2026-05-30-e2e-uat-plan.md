---
title: "E2E UAT: agent-executed walkthrough of the full surface"
type: test-plan
status: active
date: 2026-05-30
audience: ai-agent
reusable: true
target_plans:
  - docs/plans/2026-05-13-001-feat-llamatui-v1-launcher-plan.md
  - docs/plans/2026-05-18-001-feat-init-wizard-doctor-pull-plan.md
  - docs/plans/2026-05-21-001-feat-proxy-router-plan.md
---

# E2E UAT — agent-executed walkthrough (CLI · REST/proxy · `--render` TUI)

> **Executed by an AI agent.** Every step is a copy-paste command plus a
> machine-checkable assertion (exit code / `jq -e` / `grep -q` / HTTP status).
> No step says "eyeball it" — if a check can't be automated it is rewritten
> until it can, or marked ⏭️ with the reason. The plan is host-independent:
> **Setup** builds an isolated sandbox; **Fixture selection** discovers test
> models from disk. Reset the **Findings log** + **Run log** (or copy this file
> to a dated run file) before each pass.

## What is under test

Two **surfaces**, both verified programmatically:

- **Human-facing rendering** — colored/padded TTY tables and the `--render`
  TUI frames. The agent exercises TTY-gated output via the `pty` helper and
  asserts on the captured text (it does not need a real terminal).
- **Machine surface** — `--json` shapes, exit codes, and the REST/proxy
  endpoints. Asserted with `jq -e`, `$?`, and `curl` status codes.

**NOT used:** `cargo test`, the integration/e2e suite, the `--features uat`
subcommand. This reproduces what a real client hits against a release-shaped
binary; green CI can hide a broken binary, which is the gap this plan covers.

## Execution protocol (for the agent)

1. Run **§Setup**, then **§Fixture selection**. Abort the pass if the binary
   won't build or no usable fixture model is found (record in Run log).
2. Execute **§0 → §15** in order. For each item: run the command(s), evaluate
   the assertion, mark the checkbox per the legend, and on ❌/⚠️ append a row
   to the **Findings log** and reference its ID inline.
3. Prefer non-destructive ordering; launches in §5–8 reuse the sandbox daemon.
4. Run **§Teardown** at the end (and on early abort).
5. An assertion that throws (`set -e` / `jq -e` non-zero / `[ ]` false) ⇒ ❌.
   A pass that needed a documented deviation ⇒ ⚠️ with the deviation noted.

## Status legend

`- [ ]` pending · `- [x] ✅` pass · `- [x] ❌` fail (see Findings) ·
`- [x] ⏭️` skipped/blocked (reason inline) · `- [x] ⚠️` pass-with-caveat.

---

## Setup (reproducible bootstrap)

```bash
set -uo pipefail
cd "$(git rev-parse --show-toplevel)"
cargo build --bin llamastash            # release shape: add --release, adjust BIN
export BIN="$PWD/target/debug/llamastash"
export GIT_SHA="$(git rev-parse --short HEAD)"
"$BIN" --version; echo "git: $GIT_SHA"

# Isolated sandbox — never touches the user's real daemon/config.
export UAT_ROOT="${TMPDIR:-/tmp}/uat-llamastash"
rm -rf "$UAT_ROOT"; mkdir -p "$UAT_ROOT"/{state,config,cache}
export LLAMASTASH_STATE_DIR="$UAT_ROOT/state"
export LLAMASTASH_CONFIG_DIR="$UAT_ROOT/config"
export LLAMASTASH_CACHE_DIR="$UAT_ROOT/cache"
: "${HF_HOME:=$HOME/.cache/huggingface}"; export HF_HOME   # real cache = models to find

command -v llama-server >/dev/null || export LLAMASTASH_LLAMA_SERVER="$HOME/.local/bin/llama-server"

# --- Helpers (functions, so they work non-interactively) -------------------
pty()   { script -qec "$*" /dev/null; }                 # run under a pty → exercise TTY-gated rendering
rt()    { cat "$LLAMASTASH_STATE_DIR/llamastash/runtime.json"; }   # control-plane url+token
ctl()   { local u t; u=$(rt|jq -r .url); t=$(rt|jq -r .token)
          curl -s -H "Authorization: Bearer $t" -H 'content-type: application/json' -d "$1" "$u/rpc"; }
PROXY() { "$BIN" status --json | jq -r '.proxy.listen' | sed 's#^#http://#'; }   # BASE=$(PROXY)
# HTTP helpers: print status code; body saved to $HTTP_BODY
http()  { local m=$1 url=$2; shift 2; HTTP_BODY=$(mktemp)
          curl -s -o "$HTTP_BODY" -w '%{http_code}' -X "$m" "$url" "$@"; }
jchat() { jq -nc --arg m "$1" --arg p "${2:-hi}" \
          '{model:$m,messages:[{role:"user",content:$p}],max_tokens:16}'; }
```

> **First-run confirmations** (do once, record results in §1.2 / §3.3):
> the exact `runtime.json` path and field names (`url`/`token`), and the real
> `list --json` row schema. Adjust `rt()`/`ctl()` and fixture filters if they
> differ from the assumptions above.

## Fixture selection (discovery-driven)

```bash
ROOTS=("$HF_HOME/hub" /mnt/work/lmstudio-models "$HOME/.lmstudio/models")
pick() { find -L "${ROOTS[@]}" -name '*.gguf' 2>/dev/null -printf '%s\t%p\n' \
         | grep -Eiv 'mmproj' \
         | { [ -n "$1" ] && grep -Ei "$1" || cat; } \
         | { [ -n "$2" ] && grep -Eiv "$2" || cat; } \
         | sort -n | head -1 | cut -f2; }
ref() { basename "$1" .gguf; }
export CHAT_REF="$(ref "$(pick '' 'embed|rerank')")"
export EMBED_REF="$(ref "$(pick 'embed' 'rerank')")"
export RERANK_REF="$(ref "$(pick 'rerank' '')")"
export SPLIT_REF="$(ref "$(find -L "${ROOTS[@]}" -name '*-00001-of-*.gguf' 2>/dev/null | head -1)")"
export AMBIG_REF="$("$BIN" list 2>/dev/null | grep -oiE '[a-z0-9.]+-[0-9]+b' | sort | uniq -d | head -1)"
printf 'CHAT=%s\nEMBED=%s\nRERANK=%s\nSPLIT=%s\nAMBIG=%s\n' \
  "$CHAT_REF" "$EMBED_REF" "$RERANK_REF" "$SPLIT_REF" "$AMBIG_REF"
# Gate: chat fixture must resolve to exactly one model.
"$BIN" show "$CHAT_REF" >/dev/null 2>&1 && echo "CHAT_REF ok" || echo "ABORT: tighten CHAT_REF"
```

> If a role has no local model → mark that section ⏭️ or `pull` a small one
> (§13.3). **Do-not-launch:** some local models never reach `/health` on a
> given backend (e.g. large MoE on an APU); use those only for `show`/`list`
> identity tests, never as a launch fixture. `$SPLIT_REF` is inspect-only.

### Reference values (fill per pass — examples, not requirements)

| Field | This run |
|---|---|
| Host / backend | AMD Ryzen AI Max+ 395, Radeon 8060S (ROCm gfx1151) → `amd`; 121 GiB RAM, 32t |
| `llama-server` build | `9245 (b39a7bf1b)` at `~/.local/bin/llama-server` |
| `$CHAT_REF` / `$EMBED_REF` / `$RERANK_REF` | `gemma-4-E2B-it-Q4_K_M` (3.2G) / `nomic-embed-text-v1.5.Q2_K` (48M) / `qwen3-reranker-0.6b-q8_0` (610M) |
| `$SPLIT_REF` (inspect only) / `$AMBIG_REF` | `Qwen_Qwen3-Next-80B-A3B-Instruct-Q5_K_M-00001-of-00002` / `Qwen3.6-27B` (3 quants) |
| Environment note | Coexists with: **Ollama** on `:11435`, a **real release llamastash daemon** on proxy `:11436`+control `:48134`. Sandbox daemon roamed to proxy `:11437`+control `:48135`. Fully isolated via `LLAMASTASH_STATE_DIR=/tmp/uat-llamastash/state`. |

---

## 0. Build & preflight

- [x] ✅ **0.1** `cargo build --bin llamastash`; assert `$? -eq 0`. → built (incremental, exit 0).
- [x] ✅ **0.2** version matches Cargo.toml. → `llamastash 0.0.2` == `version = "0.0.2"`.
- [x] ✅ **0.3** `--help` lists all 14 subcommands + all 10 global flags. → all present.
- [x] ✅ **0.4** per-subcommand `--help` exit 0. → all 14 exit 0, no panic.
- [x] ✅ **0.5** `uat` hidden from `--help`. → not present (built without `--features uat`).
- [x] ✅ **0.6** clean slate in sandbox. → sandbox state dir empty before §1.

## 1. Daemon lifecycle

- [x] ✅ **1.1** `"$BIN" daemon start`; assert exit 0 and stdout matches `grep -qiE 'detach|background|started'`. Returns within a few seconds (not blocking). → `starting in background…` / `✓ started (detached)`, exit 0, prompt return.
- [x] ✅ **1.2** `runtime.json` shape + mode + `daemon.pid`. → mode `600` ✓, `daemon_pid` ✓. **Keys are `ipc_url`/`ipc_token`** (not `url`/`token`); file at `$LLAMASTASH_STATE_DIR/runtime.json` (no `llamastash/` subdir under env override). [F-01]
- [x] ✅ **1.3** `"$BIN" daemon status` (human) contains pid + uptime + connections. → shows name/version/protocol/pid/uptime/connections.
- [x] ✅ **1.4** `"$BIN" daemon start` again; no new pid, already-running message, single process. → `already running (pid …)`, pid unchanged, `ps -C llamastash` == 1.
- [x] ✅ **1.5** `daemon stop` → exit 0, `runtime.json` gone, pid dead.
- [x] ✅ **1.6** `daemon stop` with none running → exit 0, `daemon: not running` (graceful).
- [x] ✅ **1.7** stale runtime.json (removed while pid lingers) → client exit 65 with exact hint `Run llamastash daemon stop --force (or kill <pid>)`; `daemon stop --force` → exit 0, daemon dead.
- [x] ✅ **1.8** auto-spawn: `list` with no daemon → exit 0, daemon spawned.
- [x] ✅ **1.9** `--no-spawn` against stopped daemon → exit **65** ("not running and --no-spawn was passed").
- [x] ✅ **1.10** `daemon start --foreground` → attached ("running in foreground — Ctrl+C to stop"), runtime.json written; `kill -TERM` → clean exit + runtime.json removed.

## 2. Daemon & status (machine surface)

- [x] ✅ **2.1** `daemon status --json` shape valid; `.version`==`--version` (0.0.2).
- [x] ✅ **2.2** top-level keys all present: `daemon, external, gpu, host, models, proxy`.
- [x] ✅ **2.3** `.host` object fully populated (cpu_pct, ram_*, gpu_backend, gpu_device_count, + bonus `cpu_temp_c`, `uma_shared_*`).
- [x] ✅ **2.4** `.host.gpu_backend == "amd"` (not unsampled/unknown); `gpu_device_count == 1`.
- [x] ✅ **2.5** `.daemon.build == "0.0.2"`; `.server_path == /home/deepu/.local/bin/llama-server` (exists).
- [x] ✅ **2.6** `.proxy {enabled:true, status:"listening", listen:"127.0.0.1:11437", bind_error:null}`. Roamed 11435→11437 (Ollama on 11435, real daemon on 11436) — documented port-scan, matches `$(PROXY)`.
- [x] ✅ **2.7** Cross-check **verified, not inferred**: `ram_total_bytes` 130454011904 == `free -b` exactly; `gpu_mem_total_bytes` 69521973248 == amdgpu sysfs `vram_total`(4GiB)+`gtt_total`(60.75GiB) exactly; `uma_shared_total_bytes` == `gtt_total` exactly; `cpu_pct` 2.4 (>0). (rocm-smi shows only the 4 GiB dedicated carve-out; llamastash correctly reports the full unified VRAM+GTT pool.)
- [x] ✅ **2.8** `capabilities` returns 19 methods + `protocol_version:1`.

## 3. Discovery — `list`

- [x] ✅ **3.1** `pty` list → ANSI present, padded table `NAME ARCH QUANT CTX SIZE STATUS` + separator. (CLI `list` is flat/alphabetical; per-source grouping is TUI-only.)
- [x] ✅ **3.2** piped list → no ANSI, 6-col `\t`-separated, `awk -F'\t'` parses.
- [x] ✅ **3.3** `{"models":[…]}` (26). Schema: `arch, display_label, mode_hint, name, native_ctx, parameter_label, parent, parse_error, path, quant, source, weights_bytes`. (No `launchable` bool — projectors excluded; size=`weights_bytes`.)
- [x] ✅ **3.4** `--filter qwen` 26→7, all match, human==json.
- [x] ✅ **3.5** 1 shard-1 row, 0 shard-2 rows; `weights_bytes` == on-disk sum of both shards exactly.
- [x] ✅ **3.6** mmproj **hidden** (5 on disk → 0 rows); `mode_hint` ∈ {chat,embedding,rerank}.
- [x] ✅ **3.7** `-p <DIR>` honored at **daemon startup** (fresh daemon found symlinked model). Live-daemon client `-p` ignored — daemon owns discovery. [F-02]
- [x] ✅ **3.8** `--no-scan` honored at **daemon startup** (fresh daemon → 0 models). Live-daemon client env/flag ignored. [F-02]
- [x] ✅ **3.9** `list`(26) == `/v1/models`(26). `--render Models [N]` in §12.

## 4. Model introspection — `show`

- [x] ⚠️ **4.1** `show` (human) is rich: path/parent/source, metadata (arch, quant, native_ctx, mode_hint, parameter_label, tokenizer_kind, has_chat_template, has_reasoning_hint), per-shard size, arch_defaults (builtin `n_gpu_layers=99`), last_params. **Caveat: `quant` shows `Q6_K` for a `Q4_K_M` file** (dominant-by-bytes vs filename/`general.file_type`). [F-03]
- [x] ✅ **4.2** `show --json` valid (nested: `metadata`/`size`/`arch_defaults`/`model_id`/`last_params`/…).
- [x] ✅ **4.3** split: `shard_count:2`, `on_disk_total 53.07 GiB` summed (37.24+15.83), identity=shard 1 path.
- [x] ✅ **4.4** ambiguous `Qwen3.6-27B` → **66** + "matches 3 models" hint; bogus → **66**.
- [x] ✅ **4.5** embed `mode_hint == "embedding"`.

## 5. Launch lifecycle — chat

- [x] ✅ **5.1** `start` non-blocking: `✓ started … launch_id=L1 port=41100 pid=…`, exit 0, returns in ~1s (state=loading).
- [x] ✅ **5.2** loading→ready observed; port 41100 in range 41100–41300.
- [x] ✅ **5.3** after Ready: `latest_rss_bytes` ~2.83 GiB, `latest_cpu_pct` populated.
- [x] ❌ **5.4** `logs <name>` FAILS: `logs gemma`/`logs gemma-4-E2B-it-Q4_K_M.gguf` → "no running launch matches" though L1 is Ready+serving. Only `logs L1`/`logs 41100` work. Contradicts usage.md §Concepts (logs accepts name/path substring). [F-04]. (`logs -f` then SIGINT → exit 0 ✅.)
- [x] ✅ **5.5** BrokenPipe: `logs L1 -n 200 | head -1` → writer(logs) exit 0; SIGINT-on-follow exit 0. (Note: zsh `$pipestatus[1]`, not bash `PIPESTATUS`.)
- [x] ✅ **5.6** `last-params` records the launch params (`{"last_params":[…]}`).
- [x] ✅ **5.7** `last-params` for never-launched embed → exit **64**.
- [x] ✅ **5.8** direct chat completion to port 41100 → 200; model generates ("pong" + separate `reasoning_content`). gemma-4-E2B is a **reasoning model**: needs ≥~128 `max_tokens` for visible content (low budget → finish=length, empty content).
- [x] ✅ **5.9** `stop L2` → `stopped L2 → stopped`, exit 0, gone. (Starting an already-running model creates a 2nd launch L2/41101 — concurrent same-model launches allowed by design.)
- [x] ✅ **5.10** `--ctx 999999` → stderr `! --ctx 999999 exceeds native context length 131072 … will still try`, exit 0 (llama.cpp clamped, reached Ready).
- [x] ⏭️ **5.11** No unknown-mode fixture on disk (all models are chat/embed/rerank) — strict-mode path not triggerable here. Skipped.
- [x] ✅ **5.12** `start --port 41200` → embed L4 honored port 41200.

## 6. Embedding & rerank launches

- [x] ✅ **6.1** embed `--mode embedding` → L4 Ready.
- [x] ✅ **6.2** rerank `--mode rerank` → L5 Ready.
- [x] ✅ **6.3** `status` shows L1 chat / L4 embedding / L5 rerank, all Ready with correct modes.

## 7. Presets & favorites

- [x] ✅ **7.1** `presets save fast --ctx 4096 --reasoning off` → saved.
- [x] ⚠️ **7.2** overwrite reports `{"name":"fast","replaced":true}` — works, but it's a **boolean**, while usage.md says it reports `replaced: <old-params>`. [F-05 doc drift]
- [x] ✅ **7.3** `presets list --json` shows `fast` (ctx 8192).
- [x] ✅ **7.4** `presets show fast` → ctx 8192.
- [x] ✅ **7.5** `start --preset fast` → `(preset: fast)`, launch ctx==8192 applied.
- [x] ✅ **7.6** `presets delete fast` → removed; list empty.
- [x] ✅ **7.7** `favorites add` → favorited; `favorites list --json` shows id+name+path.
- [x] ✅ **7.8** `favorites remove` → list empty.
- [x] ✅ **7.9** stale favorite filtered: favorited a temp model → 1; deleted the file → fresh daemon lists 0.
- [x] ✅ **7.10** preset `persist-test` + favorite survive `daemon stop`+`start` (read back from `state.json`).

## 8. Multi-model & stop-all

- [x] ✅ **8.1** 3 concurrent (chat/embed/rerank) Ready, distinct ports 41100/41200/41102.
- [x] ✅ **8.2** `stop --all` non-TTY no `-y` → exit **64**, `✗ stop --all in a non-interactive context requires --yes`; all 3 still running.
- [x] ✅ **8.3** `stop --all -y --json` → `{count:3, stopped:3}`; 0 running after.
- [x] ✅ **8.4** bad ref → **66**; already-gone launch id → **66** ("no running launch matches"). (No 68 here — gone target is a no-match, not a declined-stop; 68 would need daemon to accept-then-fail.)

## 9. Proxy — OpenAI-compat

`BASE=$(PROXY)`

- [x] ✅ **9.1** `/health` 200 → `{status:"ok", models_loaded:3, models_discovered:26}`.
- [x] ✅ **9.2** `/v1/models` 200, 26 rows, `object:"model"`/`created:0`/`owned_by:"llamastash"`, **byte-order sorted** (Rust lexicographic; verify with `LC_ALL=C sort`, not locale).
- [x] ✅ **9.3** chat 200, content "pong" (max_tokens 200), **no `x-llamastash-*` header** on happy path.
- [x] ✅ **9.4** `stream:true` → SSE, 160 `data:` chunks + 1 `[DONE]`.
- [x] ✅ **9.5** `/v1/completions` 200 → " Paris." finish=stop.
- [x] ✅ **9.6** `/v1/embeddings` (embed) 200, embedding dim 768.
- [x] ✅ **9.7** `/v1/rerank` (rerank) 200, correct ranking (0.999 relevant vs 0.00001 irrelevant).
- [x] ✅ **9.8** auto-start: after `stop --all`, proxy chat → 200 in 6s, auto-started L7 Ready, content "pong".
- [x] ✅ **9.9** Fallback: served by sibling + `x-llamastash-served-by` + `x-llamastash-fallback-reason` (engineered via corrupt-GGUF that catalogs but llama-server rejects — see run notes).

## 10. Proxy — error envelopes & limits

- [x] ✅ **10.1** no model → 400 `invalid_request`/`code:model_required`.
- [x] ✅ **10.2** `$AMBIG_REF` → 400 `ambiguous_model`, `matches:3`.
- [x] ✅ **10.3** `zzzznope` → 404 `model_not_found`.
- [x] ✅ **10.4** `GET /v1/chat/completions` → 404 `not_found`.
- [x] ✅ **10.5** 2.3 MiB body → 413 `payload_too_large`.
- [x] ✅ **10.6** malformed JSON → 400 `invalid_request`.
- [x] ✅ **10.7** all stopped + broken-model request → 503 `launch_failed`, `running:[]`.

## 11. Proxy — Ollama-compat

- [x] ✅ **11.1** `GET /` → `LlamaStash is running` (default mode).
- [x] ✅ **11.2** `/api/version` `0.0.2` == `daemon.build`.
- [x] ✅ **11.3** `/api/tags` 26 models; `digest:"blake3:…"`, `modified_at:"1970-01-01T00:00:00Z"`, byte-sorted; details carry family/parameter_size/quantization_level.
- [x] ✅ **11.4** `/api/ps` 3 rows; `expires_at:"9999-12-31T23:59:59Z"`, `size_vram:0`.
- [x] ✅ **11.5** `/api/show` has `details`+`model_info`+`capabilities`.
- [x] ✅ **11.6** `daemon start --ollama-compat` → bound `127.0.0.1:11434` (free here), `GET /` → byte-exact `Ollama is running`; normal restart → 11437 + "LlamaStash is running".

## 12. Headless TUI — `--render`

`"$BIN" --render --render-size WxH > /tmp/f.txt 2>&1` then grep `/tmp/f.txt`.

- [x] ✅ **12.1** Default render: `LlamaStash v0.0.2` title + hints (help/theme/filter/quit; footer `?:help · ↹/⇧↹:panes · P:pull · t:theme · q:quit`).
- [x] ✅ **12.2** Host panel: `Host` title; bars `CPU/RAM*/GPU/VRAM`; `backend ROCm · 1 GPU` (real backend, no unsampled/unknown). Live values (CPU 12% ▲86°C, VRAM 6.0/65G).
- [x] ✅ **12.3** Daemon panel: `port 48135`, `pid`, `up`, `server …/llama-server (rocm)`, `proxy listening 127.0.0.1:11437`, `models 26 found · 1 ready`, `running 1 (… :41100)`. (Field is `port`, not `socket` — HTTP IPC; build shown in header title.)
- [x] ✅ **12.4** `Models [26]` (== list count). Source group headers + rows present.
- [x] ✅ **12.5** logo present at 130x40, **absent** at 100x30 (≥120 threshold).
- [x] ✅ **12.6** 160x50 exit 0, no panic, `Models [26]`, logo present.
- [x] ✅ **12.7** 100x30 exit 0, panels render, no panic.
- [x] ✅ **12.8** 80x25 exit 0, logo absent, no panic.
- [x] ✅ **12.9** 60x20 (floor) exit 0, full frame renders (no placeholder needed at floor).
- [x] ✅ **12.10** _(retested after fix)_ `--render-size 50x12` → exit **64** (USAGE), message `too small; minimum is 60x20`. [F-06 fixed]
- [x] ✅ **12.11** chat Ready in frame: `▶ Running` group, `● gemma-4-E2B-it-Q4_K_M … 3.2G`, right pane `ID:L1 :41100 ● ready 2.8G RAM · 0%`.
- [x] ✅ **12.12** severity glyph present (`▲` on CPU 86°C; temp tier glyphs).

## 13. Setup surfaces — `recommend`, `pull`, `init`, `doctor`

- [x] ✅ **13.1** `recommend --json` → exit 0, `steps_ran` incl. `models`, 11 recommendations for AMD.
- [x] ✅ **13.2** `recommend --offline` → exit 72, clear refusal ("cannot satisfy --only models with --offline").
- [x] ✅ **13.3** `pull ggml-org/models:tinyllamas/stories15M-q4_0.gguf --json` → exit 0, downloaded 19 MB into correct HF cache layout (`models--ggml-org--models/snapshots/<sha>/…`), JSON `{files,repo,revision,total_bytes:19077344}`.
- [x] ✅ **13.4** _(retested after fix)_ `pull <nonexistent>` → **69**. `LLAMASTASH_OFFLINE=1/true/yes` → offline refusal (69); `=0`/empty no longer error (`OFFLINE=0 list` → exit 0); `OFFLINE=1 recommend` → 72. [F-08 fixed]
- [x] ✅ **13.5** `doctor --json` exit **0**, `schema_version:1`, `findings:[]` (clean sandbox), `baseline` present, fields-complete.
- [x] ✅ **13.6** `doctor` human → exit 0, "everything looks healthy".
- [x] ✅ **13.7** `init --json --recommended --offline --skip models --llama-server <real>` → exit 0, `steps_ran:[detect,server,config,smoke,handoff]`, `hardware.gpu_backend:amd`, sandbox config only. (init refuses a **symlinked** llama-server by design → use the real path.)
- [x] ✅ **13.8** non-TTY `init --only config` (no consent) → exit **72** with exact "config-write needs explicit consent…" message; partial `init_snapshot.json` persisted.
- [x] ✅ **13.9** `init --recommended --offline --only config` → exit 0, sandbox config written, no network.
- [x] ✅ **13.10** pre-answer flags (`--config-step skip`, real binary) honored → exit 0 + handoff message.

## 14. Cross-cutting: color, env, config

- [x] ✅ **14.1** all 4 `--json` variants (pipe/pty × `--no-colors` × `NO_COLOR=1`) byte-identical (1 md5).
- [x] ✅ **14.2** `NO_COLOR=1` on pty → no ANSI; `NO_COLOR=` (empty) → ANSI present (per no-color.org).
- [x] ✅ **14.3** `--no-colors` on pty → no ANSI.
- [x] ✅ **14.4** second sandbox daemon → distinct pid + control port (48136 vs 48135), both reachable, no collision.
- [x] ✅ **14.5** `LLAMASTASH_IPC_URL`+`IPC_TOKEN` → client works, bypasses runtime.json; only-URL → exit 65 "both must be set together".
- [x] ✅ **14.6** custom config (proxy.port 11500, gruvbox theme) honored: `proxy.listen 127.0.0.1:11500`, render shows `gruvbox`.
- [x] ⚠️ **14.7** bogus **top-level** key → tolerated, rest of config still applies (theme=latte) ✅. But bogus **`[proxy]`** key is NOT "rejected loudly": daemon logs a WARN and **silently falls back to defaults for the WHOLE config** (theme reverts to macchiato), `daemon start` exits 0. Contradicts usage.md "never silently falls back to defaults." [F-09]
- [x] ✅ **14.8** `proxy.enabled:false` → `{enabled:false, listen:null, status:"disabled"}`, nothing bound.
- [x] ✅ **14.9** `-q favorites add` → empty stdout, exit 0; `-q` on a bad ref still prints `✗ no model matches…` to stderr.

## 15. Negative / robustness

- [x] ✅ **15.1** _(retested after fix)_ `init --only config --skip server` → exit **64** (clap mutual-exclusion). [F-07 fixed]
- [x] ✅ **15.2** `start` no-ref non-TTY → exit **64** "interactive start picker requires a TTY; pass an explicit argument" (no hang).
- [x] ✅ **15.3** _(retested after fix)_ invalid `--render-size` (abc/120/120x/0x0/50x12) → all **exit 64**; clap rejections (`--bogusflag`, bad subcmd, missing value) also **64**. [F-06 + F-07 fixed]
- [x] ✅ **15.4** control-plane `POST /rpc` no bearer → **401** `{"error":"unauthorized","reason":"missing Authorization header"}`; with bearer → 200.
- [x] ✅ **15.5** `logs -f` then daemon hard-kill → follow exits **65**. (Child survives via setsid.)
- [x] ✅ **15.6** _(retested after fix)_ orphan re-adoption works: daemon log `orphan sweep: 1 adopted (now external), 0 stale, 0 external`; `status.external` = **1 row** (was 36); adopted pid present. [F-10 + F-11 fixed]

---

## Teardown

```bash
"$BIN" stop --all -y 2>/dev/null || true
"$BIN" daemon stop 2>/dev/null || "$BIN" daemon stop --force 2>/dev/null || true
pkill -f "$UAT_ROOT" 2>/dev/null || true
pgrep -af llama-server || echo "no orphaned llama-server"   # kill any this run spawned
rm -rf "$UAT_ROOT"
```

---

## Findings log

IDs referenced inline from ❌/⚠️ items. **Reset per pass.**

| ID | Sev | § | Summary | Repro / assertion that failed | Status |
|---|---|---|---|---|---|
| F-01 | info | 1.2 | `runtime.json` keys are `ipc_url`/`ipc_token` and the file sits directly in `$LLAMASTASH_STATE_DIR` (no `llamastash/` subdir) under env override — plan helpers + any doc that implies `url`/`token` should say so. Not a product bug. | `jq 'keys' runtime.json` → `[daemon_pid,ipc_token,ipc_url,schema_version,started_at_unix]` | open (doc note) |
| F-02 | low | 3.7/3.8 | Client-side `-p <DIR>` / `--no-scan` / `LLAMASTASH_NO_SCAN` are **silently ignored against an already-running daemon** (daemon owns discovery; flags only bite when they influence daemon startup). Works correctly at startup. Possible UX surprise — consider a "daemon already running; scan flags ignored" notice. | `llamastash -p /foo list` vs running daemon → /foo not scanned; fresh-daemon `-p /foo` → scanned | open (UX) |
| F-03 | low | 4.1/3.1/3.3 | `quant` label wrong for mixed-K-quant large-vocab models: `dominant_quant` picks the ggml-type with most **weight-bytes**, so gemma-4-E2B-it-**Q4_K_M** reports **Q6_K** (Q6_K embedding/tensors = 2.30 GiB > Q4_K 0.85 GiB). Diverges from filename **and** `general.file_type=15 (Q4_K_M)`. Comment claims it "mirrors llama.cpp filename labels" but llama.cpp labels from `general.file_type`. Cosmetic (list/show/api/tags); no launch impact. Fix: prefer `general.file_type` when present. | header `general.file_type=15`; `dominant_quant` byte-sum → Q6_K | open (bug) |
| F-04 | med | 5.4 | `logs <name-or-path-substring>` never resolves a running launch — only `logs L<n>` / `logs <port>` work. `logs gemma`, even exact `logs gemma-4-E2B-it-Q4_K_M.gguf`, → "no running launch matches" while the launch is Ready+serving. `resolve_running()` (`src/cli/resolve.rs:543`) only does port/launch-id; no name/path branch. Contradicts `usage.md §Concepts` ("`logs`/`stop` … accept … a case-insensitive substring of the file name"). Fix: extend `resolve_running` to fall back to catalog-name→running-launch, or correct the docs. (Verify same for `stop` in §8.) | `llamastash logs gemma-4-E2B-it-Q4_K_M.gguf` → exit 66 while `logs L1` works | open (bug/doc) |
| F-05 | info | 7.2 | `presets save` overwrite returns `{"name","replaced":true}` (boolean) but `usage.md §presets` says it reports `replaced: <old-params>` so callers can audit. Either surface the old params or fix the doc. | `presets save fast --json` on existing → `replaced:true` | open (doc) |
| F-06 | low | 12.10/15.3 | Every invalid `--render-size` (parse error *and* too-small range) exits **71 (UNKNOWN)** instead of **64 (USAGE)**. Messages are correct/clear. `parse_render_size`'s `Err` bubbles via anyhow→71 instead of mapping to `USAGE`. | `llamastash --render --render-size 50x12; echo $?` → 71 | **fixed** (cli/mod.rs UNKNOWN→USAGE; retest → 64) |
| F-07 | low | 15.1 | Clap-level rejections exit **2** (clap default): `list --bogusflag`, unknown subcommand, `init --only+--skip`, missing flag value. `usage.md` exit table says "Clap also emits this [64] on its own" — but clap emits 2. Handler-level usage errors (e.g. `start` no-ref) correctly return 64. Fix doc wording or map clap errors to 64 in `main`. | `llamastash list --bogusflag; echo $?` → 2 | **fixed** (main.rs try_parse → USAGE; retest → 64) |
| F-08 | med | 13.4 | `LLAMASTASH_OFFLINE` only accepts `true`/`false`. The **documented** `LLAMASTASH_OFFLINE=1` errors with `invalid value '1' for '--offline'` (exit 2); `=0` and **empty** also error. So `export LLAMASTASH_OFFLINE=0` (or `=1` per docs) breaks **every** init/pull/recommend invocation with a clap error. Env is bound to a SetTrue flag that only bool-parses true/false. Fix: accept 1/0/empty (empty = unset, like `NO_COLOR`) or correct the docs. `=true` works (exit 69). | `LLAMASTASH_OFFLINE=1 llamastash pull x/y; echo $?` → 2 | **fixed** (cli_args::normalize_offline_env; retest → 69) |
| F-09 | med | 14.7 | A typo / unknown key inside `[proxy]` does **not** "reject loudly". serde detects it (`deny_unknown_fields`) but the handler **logs a WARN to the daemon log and silently falls back to defaults for the ENTIRE config** — `daemon start` exits 0, the user sees no error, and *all* other config (theme, port_range, …) is discarded too. Directly contradicts usage.md: "Unknown keys inside `[proxy]` are rejected loudly … a typo never silently falls back to defaults." Verified: `theme: latte` + bogus proxy key → theme reverts to macchiato. Fix: surface the parse error to the user / fail startup, or at least don't discard the whole config. | daemon log `[WARN] failed to parse config … unknown field 'bogus_proxy_key' … Using defaults.` | open (bug/doc) |
| F-10 | **med-high** | 15.6 | **Orphan re-adoption broken on current llama.cpp (b9245).** After a daemon crash/restart, a still-alive child that answers `/v1/models` with the correct model is rejected as **stale** (`orphan sweep: 0 adopted, 1 stale`). Root cause: `orphans::body_mentions_path` (`src/daemon/orphans.rs:169`) requires a `/v1/models` `data[].id` to **exactly equal the full canonical model path**, but llama.cpp b9245 returns only the **basename** as `id`. So the documented "process survival + re-adoption on restart" reliability feature does not restore managed status on the current llama-server. Fix: match `id` against the path **basename** (or `meta`), not the full path. | `kill -9 <daemon>` w/ a Ready model, restart → model not in `status.models`; log shows `0 adopted, 1 stale` | **fixed** (orphans.rs `id_matches`; retest 15.6 → `1 adopted`) |
| F-11 | med | 15.6 | **External-process discovery counts threads as separate processes.** `orphans::sweep` iterates `sys.processes()`, which includes thread **TIDs**; each thread of a llama-server inherits `comm=llama-server` + the same cmdline → one 36-thread model produces **36 `status.external[]` rows** (all same TGID, `port:null`). Pollutes `status.external`, the TUI external view, counts. Also frozen into the boot snapshot (external discovery is one-shot at startup), so stale rows persist after the process dies until daemon restart. Fix: dedupe by TGID / skip `proc.thread_kind().is_some()`. | `kill -9 <daemon>` w/ Ready model, restart → `status.external` has 36 rows for 1 process (`ps NLWP=36`) | **fixed** (skip `thread_kind().is_some()`; retest → 1 row) |

## Run log

| Field | Value |
|---|---|
| Date / runner | 2026-05-30 / AI agent (Claude), maintainer-driven |
| Binary git SHA / version | `133af7f` / `llamastash 0.0.2` (debug build) |
| Host / backend | AMD Ryzen AI Max+ 395, Radeon 8060S (ROCm gfx1151) → `amd`; 121 GiB RAM, 32t; Arch 7.0.7; llama-server b9245 |
| Fixtures (chat/embed/rerank) | `gemma-4-E2B-it-Q4_K_M` / `nomic-embed-text-v1.5.Q2_K` / `qwen3-reranker-0.6b-q8_0`; split=`Qwen3-Next-80B…00001-of-00002`; ambig=`Qwen3.6-27B` |
| Items: ✅ / ❌ / ⚠️ / ⏭️ | **116 ✅ / 4 ❌ / 5 ⚠️ / 1 ⏭️** (126 total) |
| Findings | 11 (F-01…F-11): 1 med-high (F-10 re-adoption), 4 med (F-04 logs ref, F-08 OFFLINE env, F-09 proxy-config silent-default, F-11 thread-explosion), 4 low (F-02 scan flags, F-03 quant label, F-06 render-size exit, F-07 clap exit), 2 info (F-01 runtime keys, F-05 preset replaced). No crashes, no security holes (loopback 401 enforced). |
| Notes | Ran fully isolated (`LLAMASTASH_STATE_DIR=/tmp/uat-llamastash/state`); coexisted with Ollama:11435 + real release daemon (proxy:11436/ctl:48134) which roamed the sandbox proxy→11437/ctl→48135 (exercised port-scan). 9.9 fallback engineered with a truncated GGUF. Teardown confirmed real daemon + Ollama untouched. **❌ items: 5.4 (F-04), 12.10 (F-06), 15.3 (F-06), 15.6 (F-10+F-11).** ⏭️ 5.11 (no unknown-mode fixture). Reproducible: re-run §Setup→§Teardown to repeat. |
