[package]
name = "llamatui"
version = "0.1.0-dev"
authors = ["Deepu K Sasidharan <d4udts@gmail.com>"]
description = "A fast, keyboard-driven TUI for launching local llama.cpp models"
documentation = "https://github.com/llamatui/llamatui"
keywords = ["llamatui", "llama-cpp", "llm", "tui", "gguf"]
repository = "https://github.com/llamatui/llamatui"
homepage = "https://github.com/llamatui/llamatui"
readme = "README.md"
license = "MIT"
edition = "2021"
rust-version = "1.80"
exclude = [
  "test_data/*",
  "deployment/*",
  "artwork/*",
  ".github",
  "CONTRIBUTING.md",
  "*.log",
  "tags",
]

[dependencies]
anyhow = "1.0"
blake3 = { version = "1", default-features = false, features = ["std"] }
clap = { version = "4.6", default-features = false, features = [
  "help",
  "usage",
  "error-context",
  "derive",
  "std",
] }
crossterm = "0.29"
directories = "6"
ignore = "0.4"
libc = "0.2"
log = "0.4"
notify-debouncer-mini = { version = "0.6", default-features = false }
ratatui = { version = "0.30", default-features = false, features = [
  "crossterm",
] }
regex = { version = "1.11", default-features = false, features = [
  "std",
  "perf",
  "unicode-perl",
] }
serde = { version = "1.0", default-features = false, features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"
simplelog = { version = "0.12", default-features = false }
strum = { version = "0.27", features = ["derive"] }
tokio = { version = "1.50", default-features = false, features = [
  "io-util",
  "macros",
  "net",
  "rt-multi-thread",
  "signal",
  "sync",
  "time",
] }

[lib]
name = "llamatui"
path = "src/lib.rs"

[[bin]]
name = "llamatui"
path = "src/main.rs"
bench = false

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
